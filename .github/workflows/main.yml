name: Build Docker Image

on:
  push:
    tags:
      - 'v*.*.*'
      
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
        with:
          version: 8
      # Setup Node environment
      - uses: actions/setup-node@v4
        with:
          node-version: '20.9.0'
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: |
          pnpm install

      - name: Build and push Docker image
        run: |
          docker build -t fethca/oklezzgo:latest .
          docker login -u "$DOCKER_USERNAME" -p "$DOCKER_TOKEN"
          docker push fethca/oklezzgo:latest
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_TOKEN: ${{ secrets.DOCKER_TOKEN }}

      - name: Deploy new image
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            if [ "$(docker ps -q --filter "name=oklezzgo")" ]; then docker stop oklezzgo && docker rm oklezzgo; fi
            if [ "$(docker images -q 'fethca/oklezzgo')" ]; then docker rmi fethca/oklezzgo; fi
            cd ~/traefik
            docker-compose up -d --remove-orphans oklezzgo
